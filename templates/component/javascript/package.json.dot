{
  "name": "{{=it.name}}",
  "version": "1.0.0",
  "description": "{{=it.name}} component",
  "main": "src/index.js",
  "scripts": {
    "dist": "taskr dist",
    "lint": "taskr lint",
    "prespec": "npm run lint",
    "spec": "taskr spec ",
    "clean": "taskr clean",
    "superclean": "taskr superclean",
    "devinit": "rm -f package-lock.json && npm install taskr @taskr/clear @taskr/shell @taskr/jest && taskr devinstall"
  },
  "author": "Kumori Systems (info@kumori.systems)",
  "license": "MIT",
  "devDependencies": {
    "@taskr/jest": "^1.1.1",
    "@taskr/shell": "^1.1.0",
    "jest": "^20.0.3",
    "lint": "^1.1.2",
    "taskr": "^1.1.0"
  },
  "dependencies": {
    "http-message": "git+ssh://git@gitlab.com:ECloud/http-message.git#BACO",
    "component": "git+ssh://git@gitlab.com:ECloud/component.git#BACO"
  },
  "engines": {
    "node": ">=4.2.1"
  },
  "os": [
    "linux",
    "darwin"
  ]
}
